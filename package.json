{
  "name": "ananta-browser-extension",
  "version": "1.0.0",
  "description": "Build toolchain for Ananta New Tab browser extension",
  "private": true,
  "scripts": {
    "icons": "node scripts/gen-icons.js",
    "build": "node scripts/build.js",
    "build:chrome": "node scripts/build.js --target chrome",
    "build:firefox": "node scripts/build.js --target firefox",
    "build:edge": "node scripts/build.js --target edge",
    "build:all": "npm run icons && node scripts/build.js --target all",
    "safari": "xcrun safari-web-extension-converter dist/chrome --project-location dist/safari --app-name Ananta --bundle-identifier com.kayogen.ananta --no-prompt"
  },
  "devDependencies": {
    "archiver": "^4.0.2",
    "sharp": "^0.33.4"
  }
}
